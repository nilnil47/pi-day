# Supabase: Project Settings → API (anon key is safe for browser clients).
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# After running supabase/schema.sql: SELECT id FROM competitions LIMIT 1;
VITE_COMPETITION_ID=your-uuid-here
